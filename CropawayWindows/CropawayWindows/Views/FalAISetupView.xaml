<Window x:Class="CropawayWindows.Views.FalAISetupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="fal.ai API Key Setup"
        Width="460" Height="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">

    <Border Padding="24,20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="{StaticResource IconAI}"
                           FontSize="20"
                           Foreground="{StaticResource AccentBrush}"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0" />
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="fal.ai API Key"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock Text="Required for AI-powered object tracking"
                               FontSize="11"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,2,0,0" />
                </StackPanel>
            </StackPanel>

            <!-- API Key input -->
            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                <TextBlock Text="API Key" FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,4" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Password box (masked) -->
                    <PasswordBox x:Name="ApiKeyPasswordBox"
                                 Grid.Column="0"
                                 Background="{StaticResource PanelBrush}"
                                 Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}"
                                 BorderThickness="1"
                                 Padding="8,6"
                                 FontSize="13"
                                 MaxLength="200"
                                 PasswordChanged="OnPasswordChanged">
                        <PasswordBox.Style>
                            <Style TargetType="PasswordBox">
                                <Setter Property="CaretBrush" Value="White" />
                                <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}" />
                            </Style>
                        </PasswordBox.Style>
                    </PasswordBox>

                    <!-- Plain text box (revealed) - shown when reveal is toggled -->
                    <TextBox x:Name="ApiKeyTextBox"
                             Grid.Column="0"
                             Style="{StaticResource DarkTextBox}"
                             Visibility="Collapsed"
                             FontSize="13"
                             MaxLength="200"
                             TextChanged="OnTextChanged" />

                    <!-- Reveal / hide toggle button -->
                    <Button x:Name="RevealButton"
                            Grid.Column="1"
                            Style="{StaticResource ToolbarButton}"
                            Click="OnRevealToggle"
                            ToolTip="Show/Hide API key"
                            Padding="8,6" Margin="4,0,0,0"
                            VerticalAlignment="Stretch">
                        <TextBlock x:Name="RevealIcon"
                                   FontFamily="Segoe Fluent Icons"
                                   Text="&#xE7B3;"
                                   FontSize="14" />
                    </Button>
                </Grid>
            </StackPanel>

            <!-- Validation indicator -->
            <Border Grid.Row="2" Margin="0,0,0,12"
                    CornerRadius="3" Padding="8,4">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="Transparent" />
                    </Style>
                </Border.Style>
                <StackPanel x:Name="ValidationPanel" Orientation="Horizontal">
                    <TextBlock x:Name="ValidationIcon"
                               FontFamily="Segoe Fluent Icons"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0" />
                    <TextBlock x:Name="ValidationText"
                               FontSize="11"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Border>

            <!-- Link to fal.ai -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Get your API key at "
                           FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock x:Name="FalAiLink"
                           Text="fal.ai/dashboard/keys"
                           FontSize="11"
                           Foreground="{StaticResource AccentBrush}"
                           Cursor="Hand"
                           TextDecorations="Underline"
                           MouseLeftButtonDown="OnFalAiLinkClick" />
            </StackPanel>

            <!-- Spacer -->
            <Border Grid.Row="4" />

            <!-- Action buttons -->
            <DockPanel Grid.Row="5">
                <!-- Remove button (left) -->
                <Button x:Name="RemoveButton"
                        DockPanel.Dock="Left"
                        Click="OnRemoveClick"
                        Padding="16,6"
                        Cursor="Hand"
                        IsEnabled="False">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="{StaticResource ErrorBrush}" />
                            <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="FontSize" Value="12" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background"
                                                        Value="#30F44747" />
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.4" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="Remove Key" />
                </Button>

                <!-- Save / Cancel (right) -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Click="OnCancelClick"
                            Padding="16,6" Margin="0,0,8,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{StaticResource PanelBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="FontSize" Value="12" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background"
                                                            Value="{StaticResource HoverBrush}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="Cancel" />
                    </Button>

                    <Button x:Name="SaveButton"
                            Style="{StaticResource AccentButton}"
                            Click="OnSaveClick"
                            IsEnabled="False"
                            Padding="20,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontFamily="Segoe Fluent Icons"
                                       Text="{StaticResource IconCheck}"
                                       FontSize="12" Margin="0,0,6,0"
                                       VerticalAlignment="Center" />
                            <TextBlock Text="Save" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Spacer -->
                <Border />
            </DockPanel>
        </Grid>
    </Border>
</Window>
