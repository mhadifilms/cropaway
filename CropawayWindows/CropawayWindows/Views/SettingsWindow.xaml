<Window x:Class="CropawayWindows.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CropawayWindows.ViewModels"
        mc:Ignorable="d"
        Title="Settings"
        Width="500" Height="600"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextPrimaryBrush}"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">

    <Border Padding="24,20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock FontFamily="Segoe Fluent Icons"
                           Text="{StaticResource IconSettings}"
                           FontSize="22"
                           Foreground="{StaticResource AccentBrush}"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0" />
                <TextBlock Text="Settings"
                           FontSize="18" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>

            <!-- Settings sections -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- FFmpeg Path Section -->
                    <TextBlock Text="FFMPEG" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,8" />

                    <TextBlock Text="FFmpeg executable path"
                               FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,4" />
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="FFmpegPathTextBox"
                                 Grid.Column="0"
                                 Style="{StaticResource DarkTextBox}"
                                 FontSize="12"
                                 VerticalContentAlignment="Center" />

                        <Button Grid.Column="1"
                                Style="{StaticResource ToolbarButton}"
                                Content="Browse..."
                                Click="OnBrowseFFmpeg"
                                Padding="10,6"
                                Margin="4,0,0,0" />

                        <Button Grid.Column="2"
                                Style="{StaticResource ToolbarButton}"
                                Content="Auto-detect"
                                Click="OnAutoDetectFFmpeg"
                                Padding="10,6"
                                Margin="4,0,0,0" />
                    </Grid>

                    <StackPanel x:Name="FFmpegStatusPanel" Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock x:Name="FFmpegStatusIcon"
                                   FontFamily="Segoe Fluent Icons"
                                   FontSize="12"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0" />
                        <TextBlock x:Name="FFmpegStatusText"
                                   FontSize="11"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <Separator Background="{StaticResource BorderBrush}" Height="1" Margin="0,0,0,16" />

                    <!-- fal.ai API Key Section -->
                    <TextBlock Text="FAL.AI" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,8" />

                    <TextBlock Text="API key for AI video tracking"
                               FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,4" />

                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Password box (masked) -->
                        <PasswordBox x:Name="ApiKeyPasswordBox"
                                     Grid.Column="0"
                                     Background="{StaticResource PanelBrush}"
                                     Foreground="{StaticResource TextPrimaryBrush}"
                                     BorderBrush="{StaticResource BorderBrush}"
                                     BorderThickness="1"
                                     Padding="8,6"
                                     FontSize="12"
                                     MaxLength="200"
                                     PasswordChanged="OnApiKeyPasswordChanged">
                            <PasswordBox.Style>
                                <Style TargetType="PasswordBox">
                                    <Setter Property="CaretBrush" Value="White" />
                                    <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}" />
                                </Style>
                            </PasswordBox.Style>
                        </PasswordBox>

                        <!-- Plain text box (revealed) -->
                        <TextBox x:Name="ApiKeyTextBox"
                                 Grid.Column="0"
                                 Style="{StaticResource DarkTextBox}"
                                 Visibility="Collapsed"
                                 FontSize="12"
                                 MaxLength="200"
                                 TextChanged="OnApiKeyTextChanged" />

                        <!-- Reveal / hide toggle button -->
                        <Button x:Name="RevealButton"
                                Grid.Column="1"
                                Style="{StaticResource ToolbarButton}"
                                Click="OnRevealToggle"
                                ToolTip="Show/Hide API key"
                                Padding="8,6" Margin="4,0,0,0"
                                VerticalAlignment="Stretch">
                            <TextBlock x:Name="RevealIcon"
                                       FontFamily="Segoe Fluent Icons"
                                       Text="&#xE7B3;"
                                       FontSize="14" />
                        </Button>
                    </Grid>

                    <Separator Background="{StaticResource BorderBrush}" Height="1" Margin="0,0,0,16" />

                    <!-- Export Defaults Section -->
                    <TextBlock Text="EXPORT DEFAULTS" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,8" />

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Default format -->
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Default format"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,0,8" />
                        <ComboBox x:Name="FormatComboBox"
                                  Grid.Row="0" Grid.Column="1"
                                  Background="{StaticResource PanelBrush}"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  BorderBrush="{StaticResource BorderBrush}"
                                  FontSize="12"
                                  Margin="0,0,0,8"
                                  Padding="8,5">
                            <ComboBoxItem Content="MOV" IsSelected="True" />
                            <ComboBoxItem Content="MP4" />
                        </ComboBox>

                        <!-- Default codec -->
                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Text="Default codec"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" />
                        <ComboBox x:Name="CodecComboBox"
                                  Grid.Row="1" Grid.Column="1"
                                  Background="{StaticResource PanelBrush}"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  BorderBrush="{StaticResource BorderBrush}"
                                  FontSize="12"
                                  Padding="8,5">
                            <ComboBoxItem Content="Auto (match source)" IsSelected="True" />
                            <ComboBoxItem Content="H.264" />
                            <ComboBoxItem Content="HEVC (H.265)" />
                        </ComboBox>
                    </Grid>

                    <Separator Background="{StaticResource BorderBrush}" Height="1" Margin="0,4,0,16" />

                    <!-- Theme Section -->
                    <TextBlock Text="APPEARANCE" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,0,0,8" />

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Theme"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   VerticalAlignment="Center" />
                        <ComboBox x:Name="ThemeComboBox"
                                  Grid.Column="1"
                                  Background="{StaticResource PanelBrush}"
                                  Foreground="{StaticResource TextPrimaryBrush}"
                                  BorderBrush="{StaticResource BorderBrush}"
                                  FontSize="12"
                                  Padding="8,5">
                            <ComboBoxItem Content="Dark" IsSelected="True" />
                        </ComboBox>
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <!-- Action buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Click="OnCancelClick"
                        Padding="20,6" Margin="0,0,8,0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
                            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="FontSize" Value="13" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background"
                                                        Value="{StaticResource HoverBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <TextBlock Text="Cancel" />
                </Button>

                <Button Style="{StaticResource AccentButton}"
                        Click="OnSaveClick"
                        Padding="24,6"
                        Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe Fluent Icons"
                                   Text="{StaticResource IconSave}"
                                   FontSize="12" Margin="0,0,6,0"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="Save" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
